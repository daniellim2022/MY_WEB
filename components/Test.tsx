import React from 'react';

export const TestComponent = () => {
    return (
        <div className="absolute inset-0 z-50 flex items-center justify-center bg-red-500 text-white text-4xl">
            SYSTEM CHECK: TEST COMPONENT RENDERED
        </div>
    );
};
